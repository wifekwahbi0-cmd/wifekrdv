name: wifek_rdv
description: تطبيق إدارة عيادة تقويم النطق - wifek rdv

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.0
  path: ^1.8.3
  path_provider: ^2.1.1
  intl: ^0.18.1

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
